{
  "projectId": "safehaven-security-systems",
  "orgId": "your-org-id",
  "settings": {
    "framework": "nextjs",
    "buildCommand": "cd frontend && npm install && npm run build",
    "outputDirectory": "frontend/.next",
    "installCommand": "npm install",
    "devCommand": "cd frontend && npm run dev",
    "regions": ["iad1"],
    "functions": {
      "frontend/app/api/**/*.ts": {
        "runtime": "nodejs18.x"
      }
    }
  }
}
